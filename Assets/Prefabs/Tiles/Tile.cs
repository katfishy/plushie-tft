using UnityEngine;

public class Tile : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
   public (int x, int y) coord;
   public bool occupied;
   //public Unit occupent;
}
